const customerDate=x=>new Date(x).toLocaleString('en-GB',{weekday:'short',day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Europe/London'}),statusLabel=x=>({confirmed:'Upcoming',pending:'Pending',completed:'Completed',cancelled:'Cancelled',no_show:'No-show'}[x]||x);
async function customerAccount(){const el=document.querySelector('#account'),p=new URLSearchParams(location.search),id=p.get('id'),token=p.get('token'),{data:{user}}=await sb.auth.getUser();if(id&&token&&!user)return renderGuestBooking(el,id,token);if(!user)return renderCustomerAuth(el);await sb.rpc('claim_guest_bookings');return renderCustomerDashboard(el,user)}
async function renderGuestBooking(el,id,token){const{data,error}=await sb.rpc('get_guest_booking',{p_booking_id:id,p_token:token});if(error)return el.innerHTML=`<p class="error">${esc(error.message)}</p>`;const x=(Array.isArray(data)?data:[data]).filter(Boolean)[0];if(!x)return el.innerHTML='<div class="notice">This booking link is invalid or has expired.</div>';const future=new Date(x.starts_at)>Date.now(),can=x.status==='confirmed'&&future;el.innerHTML=`<div class="customer-head"><div><span class="eyebrow">Manage booking</span><h1>${esc(x.guest_name||'Your appointment')}</h1><p>Keep this private link safe.</p></div><div class="customer-actions"><a class="btn" href="booking.html">Book another</a></div></div><section class="dash-section"><div class="customer-bookings"><article class="customer-booking"><div class="booking-date"><b>${new Date(x.starts_at).toLocaleDateString('en-GB',{day:'2-digit',timeZone:'Europe/London'})}</b><span>${new Date(x.starts_at).toLocaleDateString('en-GB',{month:'short',timeZone:'Europe/London'})}</span></div><div class="booking-info"><span class="eyebrow">${statusLabel(x.status)}</span><h3>${esc(x.service_name||'Appointment')}</h3><p>${customerDate(x.starts_at)} · ${esc(x.barber_name||'HStudio')}</p><small>${x.service_price_pence!=null?money(x.service_price_pence):''}</small></div>${can?`<div class="customer-actions"><button class="btn ghost" onclick="startReschedule('${x.id}','${token}','${x.service_id}','${x.barber_id}')">Reschedule</button><button class="btn ghost" onclick="customerCancel('${x.id}','${token}')">Cancel</button></div>`:''}</article></div><p class="notice">Changes and cancellations must be made at least 3 hours before your appointment.</p></section>`}
function renderCustomerAuth(el){const reg=location.hash==='#register';el.innerHTML=`<div class="customer-auth"><div class="auth-tabs"><button class="${reg?'':'active'}" data-auth="login">Sign in</button><button class="${reg?'active':''}" data-auth="register">Create account</button></div><div id="authbody"></div></div>`;el.querySelectorAll('[data-auth]').forEach(b=>b.onclick=()=>{el.querySelectorAll('[data-auth]').forEach(x=>x.classList.toggle('active',x===b));renderAuthForm(b.dataset.auth)});renderAuthForm(reg?'register':'login')}
function renderAuthForm(mode){const el=document.querySelector('#authbody'),reg=mode==='register';el.innerHTML=`<div class="panel account-panel"><span class="eyebrow">${reg?'New customer':'Welcome back'}</span><h2>${reg?'Create your HStudio account':'Sign in to HStudio'}</h2><p>${reg?'Manage appointments and keep your visit history together.':'View and manage your HStudio appointments.'}</p><form id="customerAuthForm" class="formgrid">${reg?`<label class="field full">Full name<input name="name" autocomplete="name" required></label><label class="field full">Mobile number<input name="phone" autocomplete="tel" required></label>`:''}<label class="field full">Email<input type="email" name="email" autocomplete="email" required></label><label class="field full">Password<input type="password" name="password" autocomplete="${reg?'new-password':'current-password'}" minlength="8" required></label>${reg?`<label class="field full account-consent"><input type="checkbox" required> I agree to HStudio using these details to manage my appointments.</label>`:''}<button class="btn field full">${reg?'Create account':'Sign in'}</button>${reg?'':`<button type="button" class="btn ghost field full" id="forgotPassword">Forgot password?</button>`}<p class="field full" id="accountmsg"></p></form></div>`;document.querySelector('#customerAuthForm').onsubmit=e=>submitCustomerAuth(e,reg);document.querySelector('#forgotPassword')?.addEventListener('click',resetPassword)}
async function resetPassword(){const email=document.querySelector('input[name="email"]').value.trim(),msg=document.querySelector('#accountmsg');if(!email)return msg.textContent='Enter your email address first.';const{error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/account.html'});msg.textContent=error?error.message:'Password reset link sent. Check your email.'}
async function submitCustomerAuth(e,reg){e.preventDefault();const f=new FormData(e.target),msg=document.querySelector('#accountmsg'),btn=e.target.querySelector('button');btn.disabled=true;msg.textContent=reg?'Creating your account…':'Signing in…';const r=reg?await sb.auth.signUp({email:f.get('email'),password:f.get('password'),options:{data:{full_name:f.get('name'),phone:f.get('phone')}}}):await sb.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});if(r.error){msg.textContent=r.error.message;btn.disabled=false;return}if(reg&&!r.data.session){msg.textContent='Account created. You can sign in once your account is active.';btn.disabled=false;return}location.reload()}
async function renderCustomerDashboard(el,user){const{data,error}=await sb.from('bookings').select('*,services(name,price_pence,duration_minutes),barbers(name)').eq('customer_id',user.id).order('starts_at',{ascending:true});if(error)return el.innerHTML=`<p class="error">${esc(error.message)}</p>`;const all=data||[],now=Date.now(),upcoming=all.filter(x=>['confirmed','pending'].includes(x.status)&&new Date(x.starts_at)>now),past=all.filter(x=>!upcoming.includes(x));el.innerHTML=`<div class="customer-head"><div><span class="eyebrow">Customer account</span><h1>Hi, ${esc(user.user_metadata?.full_name?.split(' ')[0]||'there')}.</h1><p>${esc(user.email)}</p></div><div class="customer-actions"><a class="btn" href="booking.html">Book appointment</a><button class="btn ghost" id="editProfile">Profile</button><button class="btn ghost" id="customerSignout">Sign out</button></div></div><div class="stats customer-stats"><div class="stat"><small>Upcoming</small><b>${upcoming.length}</b><span>appointments</span></div><div class="stat"><small>Total bookings</small><b>${all.length}</b><span>on this account</span></div></div><section class="dash-section"><div class="section-head"><div><span class="eyebrow">Next up</span><h2>Upcoming bookings</h2></div></div><div class="customer-bookings">${upcoming.length?upcoming.map(customerBookingCard).join(''):'<div class="empty-state"><b>No upcoming bookings</b><span>Ready for your next trim?</span><a class="btn" href="booking.html">Book now</a></div>'}</div></section>${past.length?`<section class="dash-section"><div class="section-head"><div><span class="eyebrow">History</span><h2>Previous bookings</h2></div></div><div class="customer-bookings past">${past.slice().reverse().map(customerBookingCard).join('')}</div></section>`:''}`;document.querySelector('#customerSignout').onclick=async()=>{await sb.auth.signOut();location.reload()};document.querySelector('#editProfile').onclick=()=>profileModal(user)}
function customerBookingCard(x){const can=['confirmed','pending'].includes(x.status)&&new Date(x.starts_at)>Date.now();return `<article class="customer-booking"><div class="booking-date"><b>${new Date(x.starts_at).toLocaleDateString('en-GB',{day:'2-digit',timeZone:'Europe/London'})}</b><span>${new Date(x.starts_at).toLocaleDateString('en-GB',{month:'short',timeZone:'Europe/London'})}</span></div><div class="booking-info"><span class="eyebrow">${statusLabel(x.status)}</span><h3>${esc(x.services?.name||'Appointment')}</h3><p>${customerDate(x.starts_at)} · ${esc(x.barbers?.name||'HStudio')}</p><small>${x.services?.duration_minutes||''} mins · ${x.services?.price_pence!=null?money(x.services.price_pence):''}</small></div>${can?`<div class="customer-actions"><button class="btn ghost" onclick="startReschedule('${x.id}','','${x.service_id}','${x.barber_id}')">Reschedule</button><button class="btn ghost" onclick="customerCancel('${x.id}','')">Cancel</button></div>`:''}</article>`}
async function customerCancel(id,token){if(!confirm('Cancel this appointment? HStudio requires at least 3 hours notice.'))return;await cancelBooking(id,token);location.reload()}
async function startReschedule(id,token,service,barber){const date=prompt('Choose a new date (YYYY-MM-DD)');if(!date)return;const{data,error}=await sb.rpc('get_available_slots',{p_service_id:service,p_date:date,p_barber_id:barber});if(error)return alert(error.message);const slots=Array.isArray(data)?data:[];if(!slots.length)return alert('No available times on that date.');const choices=slots.map((s,i)=>`${i+1}. ${new Date(s.starts_at||s.start_time||s).toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit',timeZone:'Europe/London'})}`).join('\n'),pick=Number(prompt(`Available times:\n${choices}\n\nEnter the number you want:`))-1;if(!slots[pick])return;const start=slots[pick].starts_at||slots[pick].start_time||slots[pick];const r=await sb.rpc('reschedule_booking',{p_booking_id:id,p_new_starts_at:start,p_token:token||null});if(r.error)return alert(r.error.message);alert('Appointment rescheduled.');location.reload()}
function profileModal(user){const name=prompt('Full name',user.user_metadata?.full_name||'');if(name===null)return;const phone=prompt('Mobile number',user.user_metadata?.phone||'');if(phone===null)return;sb.auth.updateUser({data:{full_name:name.trim(),phone:phone.trim()}}).then(({error})=>{if(error)alert(error.message);else location.reload()})}
