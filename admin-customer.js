(()=>{const original=window.openAppointment;if(!original)return;window.openAppointment=async function(id){await original(id);const modal=document.querySelector('#apptModal .appt-modal');if(!modal)return;const{data,error}=await sb.rpc('get_staff_booking_customer',{p_booking_id:id});if(error)return;const c=Array.isArray(data)?data[0]:data;if(!c)return;const box=document.createElement('div');box.className='payment-box customer-detail-box';box.innerHTML=`<span class="eyebrow">Customer details</span><label>Name <b>${esc(c.full_name||'Not provided')}</b></label><label>Mobile <b>${c.phone?`<a href="tel:${esc(c.phone)}">${esc(c.phone)}</a>`:'Not provided'}</b></label><label>Email <b>${c.email?`<a href="mailto:${esc(c.email)}">${esc(c.email)}</a>`:'Not provided'}</b></label><label>Booking type <b>${c.is_account?'HStudio account':'Guest booking'}</b></label>`;const payment=modal.querySelector('.payment-box');payment?.before(box)}}})();