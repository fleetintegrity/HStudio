(()=>{
const demoAvatars={Mason:'https://i.pravatar.cc/400?img=12',Luca:'https://i.pravatar.cc/400?img=11',Reece:'https://i.pravatar.cc/400?img=33'};
window.NC_DEMO_AVATARS=demoAvatars;
window.chooseBarber=async function(){B.step=1;const bs=await barbers(),opts=[{id:null,name:'Any barber',title:'First available',avatar_url:null},...bs];const initials=n=>(n||'A').split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase();const photo=x=>x.avatar_url||demoAvatars[x.name]||null;setPanel(`<span class="eyebrow">Step 2 of 5</span><h2>Choose your barber</h2><p>${esc(B.service.name)} · ${money(B.service.price_pence)}</p><div class="choices barber-choices">${opts.map(x=>{const src=photo(x);return `<button class="choice barber-choice">${src?`<img class="booking-barber-avatar" src="${esc(src)}" alt="${esc(x.name)}">`:`<span class="booking-barber-avatar fallback">${x.id?initials(x.name):'ANY'}</span>`}<span><b>${esc(x.name)}</b><small>${esc(x.title)}</small></span></button>`}).join('')}</div>`);document.querySelectorAll('.choice').forEach((b,i)=>b.onclick=()=>{B.barber=opts[i];chooseDate()})};
})();